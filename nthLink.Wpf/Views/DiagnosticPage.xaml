<UserControl x:Class="nthLink.Wpf.Views.DiagnosticPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:attachProperties="clr-namespace:nthLink.Wpf.AttachedProperties"
             xmlns:markupExtension="clr-namespace:nthLink.Wpf.MarkupExtension"
             xmlns:viewModels="clr-namespace:nthLink.Wpf.ViewModels"
             attachProperties:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:DiagnosticPageViewModel}">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button" 
        BasedOn="{StaticResource TransparentButton}">
            <Style.Setters>
                <Setter Property="Width" Value="180"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Border.CornerRadius" Value="15"/>
                <Setter Property="Background" Value="{StaticResource AppBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource AppForeground}"/>
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    <Grid MouseDown="Grid_MouseDown" 
          IsHitTestVisible="{Binding IsOnDiagnostics, Converter={StaticResource ReverseBooleanConverter}}">
        <Grid VerticalAlignment="Top" Margin="0,16,0,0">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Top">
                <Grid.LayoutTransform>
                    <ScaleTransform ScaleX="0.5" ScaleY="0.5"/>
                </Grid.LayoutTransform>
                <Path Data="{StaticResource IconLeftPathData}" Fill="{StaticResource AppBackground}"/>
                <Path Data="{StaticResource IconRightPathData}" Fill="{StaticResource AppBackground}"/>
            </Grid>
        </Grid>
        <ScrollViewer Margin="28,122,18,28"
                      VerticalAlignment="Top"
                      Focusable="False">
            <TextBlock TextWrapping="Wrap"
                       Margin="0,0,4,0"
                       LineStackingStrategy="BlockLineHeight"
                       LineHeight="26"
                       FontSize="14"
                       Foreground="{StaticResource AppBlack}"
                       Text="{Binding ContentString}"
                       MouseDown="TextBlock_MouseDown"/>
        </ScrollViewer>
        
        <Button Style="{StaticResource ButtonStyle}"
         VerticalAlignment="Top"
         Margin="28,392,28,0"
         Command="{Binding StartDiagnosticCommand}">
            <Grid>
                <TextBlock Text="{markupExtension:Str diagnostic_button}"
                        FontSize="16"
                        Foreground="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"/>
            </Grid>
        </Button>
        <Grid Background="{StaticResource DialogMask}" 
              Visibility="{Binding IsOnDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}">
            <!-- 前景圓環（旋轉動畫） -->
            <Ellipse Width="60" Height="60" Stroke="{StaticResource AppBackground}" StrokeThickness="6"
                 StrokeDashArray="2" StrokeStartLineCap="Round">
                <Ellipse.RenderTransform>
                    <RotateTransform Angle="0" CenterX="30" CenterY="30"/>
                </Ellipse.RenderTransform>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Ellipse.Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                      From="0" To="360" Duration="0:0:3" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
        </Grid>
    </Grid>
</UserControl>
